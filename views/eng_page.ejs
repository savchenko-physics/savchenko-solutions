<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Meta -->
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta http-equiv="content-language" content="en" />
  <meta name="keywords" content="<%= __('meta.keywords') %>" />
  <meta name="description" content="<%= __('meta.description') %>" />
  <meta name="author" content="<%= __('meta.author') %>" />
  <meta name="date" content="<%= __('meta.date') %>" scheme="YYYY-MM" />
  <meta property="og:title" content="<%= __('title') %>" />
  <meta property="og:image" content="/img/logo.png" />
  <meta property="og:description" content="<%= __('meta.ogDescription') %>" />
  <meta name="yandex-verification" content="6cfda41f74038368" />
  <link rel="icon" href="/img/logo.png" type="image/png" />
  
  <!-- jQuery -->
  <script src="/js/jquery-1.10.1.min.js"></script>

  <!-- Bootstrap 5 (via CDN) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  
  <!-- Google Fonts: Roboto -->
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet" />

  
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/main_page.css" />

  <title><%= __('title') %></title>
</head>

<body>
  <!-- ============================
       HERO / NAVIGATION SECTION
  ============================ -->
  <header class="hero-section text-white">
    <nav class="navbar navbar-expand-lg navbar-dark">
      <div class="container-fluid px-4">
        <!-- Logo + Title -->
        <a class="navbar-brand d-flex align-items-center me-auto" href="/<%= lang %>/">
          <img 
            src="/img/logo_main.svg" 
            alt="<%= __('title') %> Logo" 
            class="logo-img me-2"
          />
          <div>
            <h1 class="site-title mb-0"><%= __('title') %></h1>
            <p class="site-motto mb-0"><%= __('motto') %></p>
          </div>
        </a>

        <!-- Toggle button for mobile -->
        <button
          class="navbar-toggler d-flex align-items-center gap-2 border-0 pe-0 d-lg-none"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarSupportedContent"
          aria-controls="navbarSupportedContent"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <% if (lang === 'ru') { %>
          <!-- Summit Link (mobile only) -->
          <a href="/summit" class="nav-link text-warning fw-bold">🎉</a>
          <% } %>

          <!-- Language Switch (mobile only) -->
          <% if (lang === 'en') { %>
            <a href="/ru" class="nav-link text-white fw-bold">EN</a>
          <% } else { %>
            <a href="/en" class="nav-link text-white fw-bold">RU</a>
          <% } %>

          <!-- Login/Profile Image -->
          <% if (username) { %>
            <a href="/user/<%= username %>" class="nav-link text-white fw-bold">
              <img
                src="<%= profilePicture || '/img/profile_images/Default_placeholder.svg' %>"
                alt="Profile Picture"
                class="rounded-circle"
                width="36"
                height="36"
              />
            </a>
          <% } else { %>
            <a href="/<%= lang %>/login" class="nav-link text-white fw-bold">
              <span class="text-white fw-bold ps-1"><%= __('menu.login') %></span>
            </a>
          <% } %>
        </button>

        <!-- Right side items: language + user -->
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav ms-auto align-items-center">
            <% if (lang === 'ru') { %>
            <li class="nav-item px-2">
              <a href="/summit" class="nav-link text-warning fw-bold" style="background: rgba(255, 193, 7, 0.1); border-radius: 20px; padding: 0.5rem 1rem;">
                🎉 Summit 2025
              </a>
            </li>
            <% } %>
            <li class="nav-item px-2 language-switch">
              <% if (lang === 'en') { %>
                <a href="/ru" class="nav-link text-white fw-bold">EN</a>
              <% } else { %>
                <a href="/en" class="nav-link text-white fw-bold">RU</a>
              <% } %>
            </li>
            <li class="nav-item px-2">
              <% if (username) { %>
                <a href="/user/<%= username %>" class="d-flex align-items-center">
                  <img
                    src="<%= profilePicture || '/img/profile_images/Default_placeholder.svg' %>"
                    alt="Profile Picture"
                    class="rounded-circle"
                    width="40"
                    height="40"
                  />
                </a>
              <% } else { %>
                <a
                  href="/<%= lang %>/login"
                  class="btn btn-outline-light rounded-pill"
                >
                  <%= __('menu.login') %>
                </a>
              <% } %>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Search Bar + Hero Text -->
    <div class="container hero-content d-flex flex-column align-items-center justify-content-center text-center">
      <h2 class="hero-subtitle mb-4"><%= __('subtitle') %></h2>
      <div class="searchbar-wrapper mb-5 position-relative">
        <svg class="search-icon position-absolute" viewBox="0 0 24 24" width="20" height="20" style="left: 15px; top: 50%; transform: translateY(-50%); z-index: 1;">
          <path fill="#6c757d" d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
        </svg>
        <input
          type="text"
          id="searchInput"
          class="form-control form-control-lg searchbar-input"
          placeholder="<%= __('menu.searchPlaceholder') %>"
          style="padding-left: 45px; padding-right: 40px;"
        />
        <button 
          type="button" 
          id="clearSearch" 
          class="btn position-absolute" 
          style="right: 10px; top: 50%; transform: translateY(-50%); display: none; background: none; border: none; padding: 5px;">
          <svg viewBox="0 0 24 24" width="16" height="16">
            <path fill="#6c757d" d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
          </svg>
        </button>
        <div
          id="searchResults"
          class="search-results position-absolute bg-white border rounded shadow-sm"
          style="display: none; z-index: 1000; width: 100%;"
        >
          <ul class="list-unstyled mb-0"></ul>
        </div>
      </div>
      <p class="hero-tagline">
        <%= __('description.heroTagline') %>
      </p>
    </div>
  </header>

  <!-- ============================
       MAIN CONTENT
  ============================ -->
  <main class="container my-5">
    <!-- ============================
         SUMMIT PROMOTION SECTION
    ============================ -->
    <% if (lang === 'ru' || lang != 'ru') { %>
    <div class="summit-promotion-banner mb-5">
      <div class="row align-items-center">
        <div class="col-lg-8">
          <div class="summit-promotion-content">
            <div class="summit-badge">
              🎉 НОВОЕ СОБЫТИЕ
            </div>
            <h2 class="summit-promotion-title">
              Первый Savchenko Solutions Summit в Москве!
            </h2>
            <p class="summit-promotion-description">
              11 июня 2025 в 18:00 в парке «Крылатские холмы». Присоединяйтесь к исторической встрече community физиков и энтузиастов образования!
            </p>
            <div class="summit-promotion-details">
              <span class="summit-detail-item">📅 11 июня 2025</span>
              <span class="summit-detail-item">⏰ 18:00–19:30</span>
              <span class="summit-detail-item">📍 м. Крылатское</span>
            </div>
            <div class="summit-promotion-buttons">
              <a href="/summit" class="btn-summit-main">
                Подробнее о саммите
              </a>
              <a href="https://t.me/+J9BAjlghLrFmNjcy" target="_blank" class="btn-summit-telegram">
                💬 Telegram-группа
              </a>
            </div>
          </div>
        </div>
        <div class="col-lg-4 text-center">
          <div class="summit-promotion-visual">
            <div class="summit-countdown-mini">
              <div class="countdown-mini-item">
                <span class="countdown-mini-number" id="summit-days">--</span>
                <span class="countdown-mini-label">дней</span>
              </div>
              <div class="countdown-separator">:</div>
              <div class="countdown-mini-item">
                <span class="countdown-mini-number" id="summit-hours">--</span>
                <span class="countdown-mini-label">часов</span>
              </div>
            </div>
            <p class="summit-countdown-text">до саммита</p>
          </div>
        </div>
      </div>
    </div>
    <% } %>

    <!-- Intro Row: Left = Text, Right = YouTube Preview -->
    <div class="row align-items-center justify-content-center mb-lg-5 mb-2">
      <div class="col-lg-6 mb-4">
        <div class="content-card shadow-sm p-4">
          <p style="text-align: justify;">    
            <%- __('description.introFirst') %>
          </p>
          <p style="text-align: justify;">
            <%- __('description.introSecond') %>
          </p>
        </div>
      </div>
      <div class="col-lg-6 d-none d-lg-flex justify-content-center mb-4">
        <a href="/summit" target="_blank">
          <img
            src="/img/summit_11-06.jpg"
            alt="Watch on YouTube"
            class="img-fluid rounded-3 shadow-sm"
            style="max-width: 100%;"
          />
        </a>
      </div>
    </div>

    <!-- Team & Acknowledgments -->
    <div class="row align-items-start justify-content-center mb-5 d-none d-lg-flex">
      <!-- Only show team image on lg screens or bigger -->
      <div class="col-lg-3">
        <img
          src="https://savchenkosolutions.com/img/team.webp"
          alt="<%= __('description.coreTeam') %>"
          class="img-fluid rounded-3"
        />
      </div>
      <div class="col-lg-9">
        <div class="content-card shadow-sm p-4">
          <h3 class="mb-3"><%= __('description.acknowledgments') %></h3>
          <p><%= __('description.deepestThanks') %></p>
          <h4><%= __('description.coreTeam') %></h4>  
          <div class="core-team">
            <% if (lang === 'en') { %>
              <a href="/user/astrosander/">Aliaksandr Melnichenka</a>
              <a href="/user/emixter/">Eugene Dubrovin</a>
              <a href="/user/Luisito/">Luis D. Fernández Quintana</a>
              <a href="/user/stefan.nicov/">Stefan Nicov</a>
              <a href="/user/ar4senN/">Arsen Almashkan</a>
              <a href="/user/a.yersh/">Andrei Yersh</a>
              <a href="/user/jepkinsss/">Artem Levko</a>
              <a href="/user/diikann_/">Dzikan Mikita</a>
              <a href="/user/Ismail/">Kaiyrbek Ismail</a>
              <a href="/user/_chender_/">Kanashenka Aliaksandr</a>
            <% } else { %>
              <a href="/user/astrosander/">Александр Мельниченко</a>
              <a href="/user/emixter/">Евгений Дубровин</a>
              <a href="/user/Luisito/">Луис Д. Фернандес Кинтана</a>
              <a href="/user/stefan.nicov/">Стефан Ников</a>
              <a href="/user/ar4senN/">Арсен Алмашкан</a>
              <a href="/user/a.yersh/">Андрей Ёрш</a>
              <a href="/user/jepkinsss/">Артем Левко</a>
              <a href="/user/diikann_/">Дикан Никита</a>
              <a href="/user/Ismail/">Кайырбек Исмаил</a>
              <a href="/user/_chender_/">Конашенко Александр</a>
            <% } %>
            <a href="<%= lang === 'en' ? '/about#team' : '/ru/about#team' %>"><%= __('description.seeAll') %></a>
          </div>
        </div>
      </div>
    </div>

    <!-- Chapter & Sections + Sidebar with Top Authors & Contributions -->
    <div class="row">
      <!-- Statistics Button (visible on mobile) -->
      <div class="col-12 d-lg-none mb-4">
        <div class="problem-statements">
          <!-- Download PDF Button -->
          <a href="<%= lang === 'en' ? '/en/savchenko_en.pdf' : '/savchenko.pdf' %>" 
            class="pdf-download-btn"
            style="border-bottom-left-radius: 0px; border-bottom-right-radius: 0px;">
            <div class="pdf-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                <polyline points="14 2 14 8 20 8" />
                <line x1="16" y1="13" x2="8" y2="13" />
                <line x1="16" y1="17" x2="8" y2="17" />
                <line x1="10" y1="9" x2="8" y2="9" />
              </svg>
            </div>
            <div class="pdf-text">
              <span class="pdf-label"><%= __('description.problemStatements') %></span>
              <span class="pdf-action"><%= __('description.downloadPDF') %></span>
            </div>
          </a>

          <!-- Statistics Button -->
          <a href="/<%= lang %>/unsolved" 
             class="pdf-download-btn"
             style="border-top-left-radius: 0; border-top-right-radius: 0;">
            <div class="pdf-icon">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M5 3v16h16"/>
                <path d="M8 13v3"/>
                <path d="M12 8v8"/>
                <path d="M16 11v5"/>
              </svg>
            </div>
            <div class="pdf-text">
              <span class="pdf-label"><%= __('description.statistics') %></span>
              <span class="pdf-action"><%= __('description.viewStats') %></span>
            </div>
          </a>
        </div>
      </div>

      <!-- Main content column -->
      <div class="col-12 col-lg-8 mb-5">
        <!-- Chapters and Problems -->
        <% chapters.forEach((chapter, chapterIndex)=> {
            const hasContent = chapter.sections.some(section => 
                section.problems.some(column => column.length > 0)
            );
            if (hasContent) { %>
        <div class="chapter-block mb-4">
          <div class="d-flex align-items-center mb-3 flex-wrap justify-content-between gap-2">
            <div class="d-flex align-items-center flex-wrap">
              <h4 class="chapter-title me-2 text-secondary mb-0 d-inline">
                <%= __('chapter') %> <%= chapterIndex + 1 %>:
              </h4>
              <h4 class="chapter-name mb-0 d-inline">
                <%= chapter.title %>
              </h4>
            </div>
            <!-- Upload Button -->
            <a
              href="/<%= lang %>/upload"
              class="edit-button text-decoration-none d-inline-flex align-items-center btn btn-sm btn-outline-primary rounded-pill"
              data-chapter="<%= chapterIndex + 1 %>"
            >
              <span class="upload-text me-2"><%= __('upload.upload') %></span>
              <svg class="upload-icon" viewBox="0 0 24 24" width="14" height="14">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4M17 8l-5-5-5 5M12 3v12"></path>
              </svg>
            </a>
          </div>
          <hr />
          <!-- Sections -->
          <% chapter.sections.forEach((section, sectionIndex) => {
               const hasProblems = section.problems.some(column => column.length > 0);
               if (hasProblems) { %>
          <div class="mb-4">
            <h5 class="section-title mb-2" id="<%= section.number %>">
              § <%= section.number %> - <%= section.title %>
            </h5>
            <div class="problems-grid">
              <% const allProblems = section.problems.flat(); 
                 allProblems.forEach((problem) => { %>
              <div class="problem-item">
                <a href="/<%= lang %>/<%= problem %>" class="problem-link">
                  <%= problem %>
                </a>
              </div>
              <% }); %>
            </div>
          </div>
          <% } }); %>
        </div>
        <% } }); %>
      </div>

      <!-- Sidebar column -->
      <div class="col-12 col-lg-4">
        <!-- Problem Statements PDF (visible on desktop) -->
        <div class="problem-statements mb-4 d-none d-lg-block">
          <div class="buttons-container bg-white rounded-3">
            <!-- PDF Download Button -->
            <a href="<%= lang === 'en' ? '/en/savchenko_en.pdf' : '/savchenko.pdf' %>" 
               class="pdf-download-btn" style="border-top-left-radius: 12px; border-top-right-radius: 12px; border-bottom-left-radius: 0; border-bottom-right-radius: 0;">
              <div class="pdf-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
                  <polyline points="14 2 14 8 20 8" />
                  <line x1="16" y1="13" x2="8" y2="13" />
                  <line x1="16" y1="17" x2="8" y2="17" />
                  <line x1="10" y1="9" x2="8" y2="9" />
                </svg>
              </div>
              <div class="pdf-text">
                <span class="pdf-label"><%= __('description.problemStatements') %></span>
                <span class="pdf-action"><%= __('description.downloadPDF') %></span>
              </div>
            </a>

            <!-- Statistics Button -->
            <a href="/<%= lang %>/unsolved" 
               class="pdf-download-btn"
               style="border-radius: 0 0 12px 12px;">
              <div class="pdf-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5">
                  <path d="M5 3v16h16"/>
                  <path d="M8 13v3"/>
                  <path d="M12 8v8"/>
                  <path d="M16 11v5"/>
                </svg>
              </div>
              <div class="pdf-text">
                <span class="pdf-label"><%= __('description.statistics') %></span>
                <span class="pdf-action"><%= __('description.viewStats') %></span>
              </div>
            </a>
          </div>
        </div>

        <!-- Top Authors -->
        <div class="top-authors text-center br-10 shadow-sm d-none d-lg-block">
            <h3 class="text-center mb-4"><%= __('description.topAuthors') %></h3>
            <!-- Wrap table in table-responsive to allow horizontal scrolling on mobile -->
            <div class="table-responsive">
                <table class="table table-hover mx-auto">
                    <thead class="thead-light">
                        <tr>
                            <th scope="col" style="width: 10%; opacity: 0.8;">#</th>
                            <th scope="col" style="width: 45%; opacity: 0.8;"><%= __('description.profile') %></th>
                            <th scope="col" style="width: 45%; opacity: 0.8;"><%= __('description.contributions') %></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% topAuthors.forEach((author, index) => { %>
                            <tr>
                                <td>
                                    <% if (index === 0) { %>🥇
                                    <% } else if (index === 1) { %>🥈
                                    <% } else if (index === 2) { %>🥉
                                    <% } else { %><%= index + 1 %>
                                    <% } %>
                                </td>
                                <td>
                                    <a href="/user/<%= author.username %>" 
                                       class="text-dark text-decoration-none">
                                        <%= author.username %>
                                    </a>
                                </td>
                                <td><%= author.rank %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- Recent Contributions -->
        <div class="contributions text-center mt-3 bg-white br-10 p-4 shadow-sm d-none d-lg-block">
            <h3 class="text-center mb-4"><%= __('description.contributions2') %></h3>
            <div class="contribution-history timeline ms-2">
                <% recentContributions.forEach((contribution, index) => { %>
                <div class="timeline-item" style="opacity: <%= index >= recentContributions.length - 3 ? Math.max(0.5, 1 - ((index - (recentContributions.length - 3)) * 0.15)) : 1 %>;">
                    <div class="timeline-content nbl-bg">
                        <strong><%= contribution.version %></strong> 
                        <% if (lang === 'en') { %>
                        <span class="text-muted">
                            <a href="/<%= lang %>/contributions/<%= contribution.id %>" class="text-primary text-decoration-none">
                                <%= lang === 'en' ? 'edited' : 'изменена' %>
                            </a> 
                            <span class="text-dark font-weight-bold"><%= contribution.editor %></span>
                        </span>
                        <% } else { %>
                        <span class="text-muted">
                            <a href="/<%= lang %>/contributions/<%= contribution.id %>" class="text-primary text-decoration-none">
                                <%= lang === 'en' ? 'edited' : 'изменена' %>
                            </a> 
                            <span class="text-dark font-weight-bold"><%= contribution.editor %></span>
                        </span>
                        <% } %>
                        <br>
                        <span style="font-size: 0.8rem; color: #888;"><%= new Date(contribution.timestamp).toISOString() %></span>
                    </div>
                </div>
                <% }); %>
            </div>
        </div>
      </div>
    </div>
  </main>

  <!-- ============================
       POP-UP & OVERLAY
  ============================ -->
  <div id="edit-popup">
    <h3><%= __('popup.title') %></h3>
    <input
      type="text"
      id="problem-name"
      placeholder="<%= __('popup.placeholder') %>"
    />
    <button id="submit-problem"><%= __('popup.submit') %></button>
    <button id="cancel-edit"><%= __('popup.cancel') %></button>
  </div>
  <div id="popup-overlay"></div>

  <!-- ============================
       FOOTER
  ============================ -->
  <footer class="bg-dark text-white py-5 border-top mt-5">
    <div class="container">
      <div class="row align-items-center text-center text-md-start">
        <div class="col-md-6 mb-4 mb-md-0">
          <!-- Brand / Title -->
          <a href="/<%= lang %>/" class="d-inline-flex align-items-center text-decoration-none mb-3">
            <img
              src="/img/logo_main.svg"
              alt="<%= __('title') %> Logo"
              style="height: 40px;"
              class="me-2"
            />
            <span class="fw-bold h5 mb-0 text-white"><%= __('title') %></span>
          </a>
          <!-- Copyright & Basic Info -->
          <p class="mb-0">
            <small>
              © <strong>Savchenko Solutions</strong>, 2023-2025
              <br />
              All solutions are contributed by their respective authors and are
              based on original textbook content, which remains the property of
              the textbook authors.
            </small>
          </p>
        </div>
        
        <!-- License & Contact -->
        <div class="col-md-6 text-md-end">
          <p class="small mb-2">
            Unless otherwise noted, solutions are provided under the
            <strong>CC BY-SA 4.0</strong> license.
            Commercial use of any material requires written permission.
          </p>
          <p class="small mb-0">
            Contact: 
            <a
              href="mailto:aliaksandr@melnichenka.com"
              class="text-decoration-none text-white"
            >
              aliaksandr@melnichenka.com
            </a>
          </p>
        </div>
      </div>
    </div>
  </footer>

  <!-- ============================
       SCRIPTS
  ============================ -->
  <style>
    .search-results {
        text-align: left;
        font-size: 0.85rem;
    }
    .search-results ul {
        padding: 0;
        margin: 0;
    }
    .search-results li {
        padding: 8px 12px;
        cursor: pointer;
    }
    .search-results li:hover {
        background-color: #f1f1f1;
    }
    .bg-dark {
      animation: fadeInFooter 0.8s ease-in-out;
    }
    @keyframes fadeInFooter {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    /* Add these mobile-specific styles */
    @media (max-width: 768px) {
        .site-title {
            font-size: 1.1rem;
        }
        .site-motto {
            font-size: 0.66rem;
        }
        .hero-subtitle {
            font-size: 1.5rem;
        }
        .hero-tagline {
            font-size: 0.9rem;
        }
        .logo-img {
            width: 36px;
            height: auto;
        }
        .searchbar-input {
            font-size: 1rem;
            padding: 0.5rem 2.5rem;
        }
        .chapter-title, .chapter-name {
            font-size: 1.2rem;
        }
        .section-title {
            font-size: 1rem;
        }
        .problem-item {
            font-size: 0.9rem;
        }
    }

    @media (max-width: 768px) {
      .problems-grid {
        display: grid;
        gap: 0.5rem;
        grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
      }
    }

    /* Summit Promotion Styles */
    .summit-promotion-banner {
      background: linear-gradient(135deg, #ff6b35 0%, #1a659e 100%);
      border-radius: 25px;
      padding: 3rem 2.5rem;
      color: white;
      position: relative;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(255, 107, 53, 0.3);
      margin-bottom: 3rem;
      animation: summitSlideIn 1s ease-out;
    }

    .summit-promotion-banner::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="summitGrain" width="50" height="50" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="white" opacity="0.1"/><circle cx="10" cy="40" r="0.5" fill="white" opacity="0.08"/></pattern></defs><rect width="100" height="100" fill="url(%23summitGrain)"/></svg>');
      animation: summitFloat 15s infinite linear;
    }

    @keyframes summitSlideIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    @keyframes summitFloat {
      0% { transform: translate(0, 0) rotate(0deg); }
      100% { transform: translate(-50px, -50px) rotate(360deg); }
    }

    .summit-promotion-content {
      position: relative;
      z-index: 2;
    }

    .summit-badge {
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.3);
      border-radius: 50px;
      padding: 0.5rem 1.2rem;
      font-size: 0.85rem;
      font-weight: 600;
      display: inline-block;
      margin-bottom: 1rem;
      animation: summitPulse 2s infinite;
    }

    @keyframes summitPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    .summit-promotion-title {
      font-size: 2.2rem;
      font-weight: 800;
      margin-bottom: 1rem;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
    }

    .summit-promotion-description {
      font-size: 1.1rem;
      margin-bottom: 1.5rem;
      opacity: 0.95;
      line-height: 1.6;
    }

    .summit-promotion-details {
      display: flex;
      gap: 1.5rem;
      margin-bottom: 2rem;
      flex-wrap: wrap;
    }

    .summit-detail-item {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      padding: 0.8rem 1.2rem;
      font-size: 0.9rem;
      font-weight: 500;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .summit-promotion-buttons {
      display: flex;
      gap: 1rem;
      flex-wrap: wrap;
    }

    .btn-summit-main {
      background: white;
      color: #ff6b35;
      border: none;
      padding: 1rem 2rem;
      border-radius: 50px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
      box-shadow: 0 8px 25px rgba(255, 255, 255, 0.3);
    }

    .btn-summit-main:hover {
      transform: translateY(-3px);
      box-shadow: 0 12px 35px rgba(255, 255, 255, 0.4);
      color: #ff6b35;
      text-decoration: none;
    }

    .btn-summit-telegram {
      background: transparent;
      color: white;
      border: 2px solid white;
      padding: 1rem 2rem;
      border-radius: 50px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .btn-summit-telegram:hover {
      background: white;
      color: #ff6b35;
      transform: translateY(-3px);
      text-decoration: none;
    }

    .summit-promotion-visual {
      position: relative;
      z-index: 2;
    }

    .summit-countdown-mini {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
      margin-bottom: 1rem;
    }

    .countdown-mini-item {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      border-radius: 15px;
      padding: 1rem 1.2rem;
      text-align: center;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }

    .countdown-mini-number {
      display: block;
      font-size: 1.8rem;
      font-weight: 700;
      font-family: 'JetBrains Mono', monospace;
    }

    .countdown-mini-label {
      display: block;
      font-size: 0.7rem;
      opacity: 0.8;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .countdown-separator {
      font-size: 1.5rem;
      font-weight: 700;
      opacity: 0.8;
    }

    .summit-countdown-text {
      font-size: 1rem;
      opacity: 0.9;
      margin: 0;
      font-weight: 500;
    }

    @media (max-width: 768px) {
      .summit-promotion-banner {
        padding: 2rem 1.5rem;
      }
      
      .summit-promotion-title {
        font-size: 1.8rem;
      }
      
      .summit-promotion-details {
        gap: 0.8rem;
      }
      
      .summit-detail-item {
        font-size: 0.8rem;
        padding: 0.6rem 1rem;
      }
      
      .summit-promotion-buttons {
        flex-direction: column;
        align-items: stretch;
      }
      
      .countdown-mini-item {
        padding: 0.8rem 1rem;
      }
      
      .countdown-mini-number {
        font-size: 1.5rem;
      }
    }
  </style>
  
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script>
    const searchInput = document.getElementById("searchInput");
    const searchResults = document.getElementById("searchResults");
    const searchResultsList = document.querySelector("#searchResults ul");
    const clearButton = document.getElementById("clearSearch");
    let lastSearchResults = ""; // Store the last search results

    // Add click event listener to document
    document.addEventListener('click', function(event) {
        // Check if click is outside search input and search results
        if (!searchInput.contains(event.target) && !searchResults.contains(event.target)) {
            searchResults.style.display = "none";
        }
    });

    // Show previous results when focusing on search input
    searchInput.addEventListener("focus", () => {
        if (searchInput.value.trim().length > 0 && lastSearchResults) {
            searchResultsList.innerHTML = lastSearchResults;
            searchResults.style.display = "block";
        }
    });

    searchInput.addEventListener("input", async () => {
        const query = searchInput.value.trim();
        const currentLang = '<%= lang %>'; // Get current language from EJS template

        if (query.length === 0) {
            searchResults.style.display = "none";
            searchResultsList.innerHTML = "";
            lastSearchResults = ""; // Clear stored results
            return;
        }

        try {
            const response = await fetch(`/search?q=${encodeURIComponent(query)}&lang=${currentLang}`);
            const data = await response.json();

            if (data.results.length > 0) {
                const limitedResults = data.results.slice(0, 10);
                const resultsHTML = limitedResults.map((result, index) => `
                    <li id="cdx-menu-item-${index}" role="option" class="cdx-menu-item cdx-menu-item--enabled">
                        <a href="${result.relativePath}" class="cdx-menu-item__content text-dark text-decoration-none d-flex align-items-center">
                            <svg class="me-2" viewBox="0 0 24 24" width="16" height="16">
                                <path fill="#9e9e9e" d="M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z"/>
                            </svg>
                            <span class="cdx-menu-item__text d-flex justify-content-between align-items-center w-100">
                                <span class="cdx-search-result-title">
                                    <strong>${result.name}</strong>
                                    <span class="cdx-menu-item__text__description"><bdi>${result.snippet}</bdi></span>
                                </span>
                                ${result.confidence === 'high' ? '<small class="ms-2" style="color: #9e9e9e; font-size: 0.7em;">★</small>' : ''}
                            </span>
                        </a>
                    </li>
                `).join("");
                
                searchResultsList.innerHTML = resultsHTML;
                lastSearchResults = resultsHTML; // Store the results
                searchResults.style.display = "block";
            } else {
                const noResultsHTML = `
                    <li class="cdx-menu-item cdx-menu-item--enabled p-4">
                        <div class="text-center">
                            <!-- No Results Icon -->
                            <svg class="mb-3" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="#6c757d" stroke-width="1.5">
                                <circle cx="11" cy="11" r="8"/>
                                <path d="M21 21l-4.35-4.35"/>
                                <path d="M8 11h6"/>
                            </svg>
                            
                            <!-- No Results Message -->
                            <h6 class="mb-3 text-secondary">
                                <%= lang === 'ru' ? 'Ничего не найдено.' : 'No results found.' %>
                            </h6>
                            
                            <!-- Action Buttons -->
                            <div class="d-flex gap-2 justify-content-center" style="white-space: nowrap;">
                                <a href="/<%= lang %>/upload" 
                                   class="edit-button btn btn-outline-light rounded-pill px-3" 
                                   style="background-color: #007bff; color: white; font-size: 0.85rem;" 
                                   data-chapter="1">
                                    <svg class="me-2" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <path d="M21 15v4a2 2 0 01-2 2H5a2 2 0 01-2-2v-4"/>
                                        <polyline points="17 8 12 3 7 8"/>
                                        <line x1="12" y1="3" x2="12" y2="15"/>
                                    </svg>
                                    <%= __('menu.upload') %>
                                </a>
                                <a href="/global-search?search=${encodeURIComponent(query)}" 
                                   class="btn btn-outline-light rounded-pill px-3" 
                                   style="border-color: #6c757d; color: #6c757d; font-size: 0.85rem;">
                                    <svg class="me-2" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                                        <circle cx="11" cy="11" r="8"/>
                                        <line x1="21" y1="21" x2="16.65" y2="16.65"/>
                                        <line x1="11" y1="8" x2="11" y2="14"/>
                                        <line x1="8" y1="11" x2="14" y2="11"/>
                                    </svg>
                                    <%= __('menu.advancedSearch') %>
                                </a>
                            </div>
                        </div>
                    </li>
                `;
                searchResultsList.innerHTML = noResultsHTML;
                lastSearchResults = noResultsHTML; // Store the no results message
                searchResults.style.display = "block";
            }
        } catch (error) {
            console.error('Error fetching search results:', error);
        }
    });

    // Show/hide clear button based on input
    searchInput.addEventListener("input", function() {
        clearButton.style.display = this.value ? "block" : "none";
    });

    // Clear search input and results
    clearButton.addEventListener("click", function() {
        searchInput.value = "";
        searchResults.style.display = "none";
        searchResultsList.innerHTML = "";
        this.style.display = "none";
        searchInput.focus();
    });
  </script>

  <!-- Pop-up handling -->
  <script>
    document.addEventListener("DOMContentLoaded", () => {
        // Add preload class to body initially
        document.body.classList.add('preload');
        
        // Remove preload class after a delay
        setTimeout(() => {
            document.body.classList.remove('preload');
        }, 500);

        const popup = document.getElementById("edit-popup");
        const overlay = document.getElementById("popup-overlay");
        const problemInput = document.getElementById("problem-name");
        let currentChapter;

        function showPopup() {
            popup.style.display = "block";
            overlay.style.display = "block";
            // Force reflow
            popup.offsetHeight;
            overlay.offsetHeight;
            // Add active class to trigger animations
            popup.classList.add("active");
            overlay.classList.add("active");
        }

        function hidePopup() {
            popup.classList.remove("active");
            overlay.classList.remove("active");
            // Wait for animations to complete before hiding elements
            setTimeout(() => {
                popup.style.display = "none";
                overlay.style.display = "none";
                problemInput.value = "";
            }, 300);
        }

        document.querySelectorAll(".edit-button").forEach(button => {
            button.addEventListener("click", (event) => {
                // Remove this event handler since we want the link to work normally
                // event.preventDefault();
                // window.location.href = `/${lang}/upload`;
            });
        });

        document.getElementById("cancel-edit").addEventListener("click", () => {
            hidePopup();
        });

        document.getElementById("submit-problem").addEventListener("click", () => {
            const problemName = problemInput.value.trim();
            if (problemName) {
                fetch("/create-problem", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({ 
                        problemName, 
                        chapter: currentChapter,
                        lang: '<%= lang %>'
                    })
                })
                    .then(response => response.json())
                    .then(data => {
                        if (data.redirectUrl) {
                            window.location.href = data.redirectUrl;
                        } else {
                            alert(data.message);
                            hidePopup();
                        }
                    })
                    .catch(error => {
                        console.error("Error:", error);
                        alert("An error occurred. Please try again.");
                    });
            } else {
                alert('<%= lang === "ru" ? "Пожалуйста, введите корректное название задачи." : "Please enter a valid problem name." %>');
            }
        });
    });
  </script>

  <!-- Convert UTC timestamps to local timezone -->
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      document.querySelectorAll('.timeline-content span[style*="font-size: 0.8rem"]').forEach(span => {
        const isoDate = new Date(span.textContent);
        const localDate = isoDate.toLocaleString(undefined, {
          month: 'short',
          day: '2-digit',
          hour: '2-digit',
          minute: '2-digit',
          hour12: true
        });
        span.textContent = localDate;
      });
    });

    // Summit Countdown Timer
    function updateSummitCountdown() {
      const eventDate = new Date('2025-06-11T18:00:00+03:00');
      const now = new Date();
      const diff = eventDate - now;

      if (diff > 0) {
        const days = Math.floor(diff / (1000 * 60 * 60 * 24));
        const hours = Math.floor((diff % (1000 * 60 * 60 * 24)) / (1000 * 60 * 60));

        const summitDaysEl = document.getElementById('summit-days');
        const summitHoursEl = document.getElementById('summit-hours');
        
        if (summitDaysEl) summitDaysEl.textContent = days.toString().padStart(2, '0');
        if (summitHoursEl) summitHoursEl.textContent = hours.toString().padStart(2, '0');
      } else {
        const summitDaysEl = document.getElementById('summit-days');
        const summitHoursEl = document.getElementById('summit-hours');
        
        if (summitDaysEl) summitDaysEl.textContent = '🎉';
        if (summitHoursEl) summitHoursEl.textContent = '🎉';
      }
    }

    // Update summit countdown every minute
    if (document.querySelector('.summit-countdown-mini')) {
      updateSummitCountdown();
      setInterval(updateSummitCountdown, 60000);
    }
  </script>
</body>
</html>