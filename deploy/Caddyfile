# The Caddyfile is an easy way to configure your Caddy web server.
#
# Unless the file starts with a global options block, the first
# uncommented line is always the address of your site.
#
# To use your own domain name (with automatic HTTPS), first make
# sure your domain's A/AAAA DNS records are properly pointed to
# this machine's public IP, then replace ":80" below with your
# domain name.
{
	servers {
		protocols h1
	}
}

#savchenkosolutions.com {
# Set this path to your site's directory.
#rrootoot * /usr/share/caddy

# Enable the static file server.
#file_server

# Another common task is to set up a reverse proxy:
#	reverse_proxy localhost:3000
#
# Or serve a PHP site through php-fpm:
# php_fastcgi localhost:9000
#}


savchenkosolutions.com {
	log {
		output file /var/log/caddy/access.log {
			roll_size     20MiB
			roll_keep     5
			roll_keep_for 168h
		}
		format json
	}

	encode zstd gzip

	header {
		-X-Powered-By
	}

	# Всё обслуживает Node, включая статику (express.static).
	# root+file_server были удалены: Caddy ставит reverse_proxy перед
	# file_server, поэтому file_server никогда не вызывался.
	reverse_proxy localhost:3000
}

#savchenkosolutions.com {
#	encode zstd gzip

#	@static {
#		path /js/* /css/* /img/* /public/* /logo_main.svg /yt_preview.jpg /favicon.ico
#	}
#	handle @static {
#		root * /home/ubuntu/savchenko-solutions
#		file_server
#		header Cache-Control "public, max-age=31536000, immutable"
#	}
#      root * /var/www/savchenko-solutions

#	handle {
#		reverse_proxy localhost:3000
#	}
#}

sandbox.savchenkosolutions.com {
	reverse_proxy localhost:4000
}

trajecta.org {
	reverse_proxy localhost:5000
}

# Refer to the Caddy docs for more information:
# https://caddyserver.com/docs/caddyfile
